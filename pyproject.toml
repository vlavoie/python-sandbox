[project]
name = "pasokon"
version = "0.1.0"
description = ""
authors = [
    {name = "vlavoie",email = "vincent.lavoie@dormakaba.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click (>=8.4.1,<9.0.0)"
]

[project.scripts]
pasokon = "pasokon.cli:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
